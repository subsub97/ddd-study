### 단일 모델의 단점
식별자를 이용해서 애그리거트를 참조하는 방식을 사용하면 즉시 로딩 방식과
같은 JPA의 쿼리 관련 최적화 기능을 사용할 수 없다.

이외에도 연관을 직접 참조하는 방식으로 연결해도 고민이 생긴다.

이는 시스템 상태를 변경할 때와 조회할 때 단일 도메인 모델을 사용하기 때문이다.

> 이를 위해 조회 모델과 상태 변경을 위한 모델을 분리하자


## CQRS 
도메인 모델 관점에서 상태 변경 기능은 주로 한 애그리거트의 상태를 변경한다.
반면 조회 기능에 필요한 데이트를 표시하려면 두 개 이상의 애그리거트가 필요할 때가 많다.
> CQRS는 복잡한 도메인에 적합하다.

JPA 기반 단일 도메인 모델을 사용하면 통계 값을 빠르게 조회하기 위해 JPA와 관련된
다양한 성능 관련 기능을 모델에 적용해야 하는데, 이런 도메인에 CQRS가 적합하다.
> 각 모델에 맞는 구현 기술을 선택할 수 있다.
명령 모델은 JPA, 조회 모델은 마이바티스를 선택 or  RDBMS 와 NoSQL을 함께 사용


### CQRS 장단점
- 장점
  - 명령 모델을 구현할 때 도메인 자제체 집중할 수 있다.
  - 명령 모델에서 조회 관련 로직이 사라져 복잡도가 낮아진다.
  - 조회 성능을 향상시키는 데 유리하다.

- 단점
  - 구현해야 할 코드가 더 많아진다. (복잡도와 구현비용 트레이드오프를 따져보자)
  - 더 많은 구현 기술이 필요하다. 경우에 따라 저장소가 달라질 수 있고, 데이터 동기화를 처리해야 함
  - 
