# 도메인 모델과 바운디드 컨텍스트

### 도메인 모델과 경계
한 개의 모델로 여러 하위 도메인을 모두 표현하려고 시도하면 오히려 모든 하위 도메인에 맞지 않는 모델을 만들게 된다.

하위 도메인마다 같은용어라도 의미가 다르고 같은 대상이라도 지칭하는 용어가 다를수 있기 때문에 한 개의 모델로 하위
도메인을 표현하려는 시도는 올바른 방법이 아니며 표현할 수 도 없다.

같은 제품이라도 카탈로그 컨텍스트와 재고 컨텍스트에서 의미가 서로 다르다.
이렇게 구분되는 경계를 갖는 컨텍스트를 DDD에서는 바운디드 컨텍스트라고 부른다.

### 바운디드 컨텍스트
바운디트 컨텍스트는 용어를 기준으로 구분한다.
한 바운디느 컨텍스트는 실제로 사용자에게 기능을 제공하는 물리적 시스템으로
도메인 모델은 이 바운디드 컨텍스트 안에서 도메인을 구현한다.

 소규모인 경우 여러 하위 도메인을 한 개의 바운디드 컨텍스트에서 구현한다.

여러 하위 도메인을 하나의 바운디드 컨텍스트 안에서 개발할 때 주의할 점은 하위 도메인 모델이
섞이지 않도록 하는 것이다.

> 하위 도메인마다 구분되는 패키지를 갖도록 구현하면 도메인 모델이 서로 뒤섞이지 않고
> 하위 도메인마다 바운디드 컨텍스트를 갖는 효과를 낼 수 있다.

이 때 나눠서 구현하면서 회원 바운디드 컨텍스트와 주문 바운디드 컨텍스트가 존재한다면 
회원 바운디드 컨텍스트의 회원과 동일한 사용자지만 주문에서는 주문자라는 VO로 사용될 수 있다.

### 바운디드 컨텍스트 구현
바운디트 컨텍스트는 도메인 모델만 포함하는 것이 아니다.
도메인 기능을 사용자에게 제공하는 데 필요한 표현 영역, 응용 서비스, 인프라스트럭처 영역을 모두 포함한다.

모든 바운디드 컨텍스트를 반드시 도메인 주도로 개발할 필요는 없다.
> 복잡한 도메인은 DDD, 아닌 경우 단순 CRUD 방식 가능

각 바운디드 컨텍스트는 서로 다른 구현 기술을 사용할 수 있다.
> MSA나 모듈러 모놀리스 같은 방식으로 하면 나중에 분리도 쉬울듯?

바운디드 컨텍스트가 반드시 사용자에게 보여지는 UI룰 가지고 있어야 하는 것은 아니다.


### 바운디드 컨텍스트 간 통합
"사용자가 제품 상세 페이지를 볼 때,보고 있는 상품과 유사한 상품 목록을 하단에 보여준다."
같은 경우 사용자가 카탈로그 BC에 추천 제품 목록을 요청하면 카탈로그 BC 컨텍스트는 추천 BC로부터 
추천 정보를 읽어와 추천 제품 목록을 제공한다.

이 때 두 BC의 도메인 모델은 서로 다르다.

**통합 방식1 - 직접 통합 (REST API)**
```java
// domain 영역 — 도메인 관점의 인터페이스
public interface ProductRecommendationService {
    List<Product> getRecommendationsOf(ProductId id);
}

// infra 영역 — 외부 연동 + 모델 변환 담당
public class RecSystemClient implements ProductRecommendationService {
    public List<Product> getRecommendationsOf(ProductId id) {
        List<RecommendationItem> items = getRecItems(id.getValue());
        return toProducts(items);  // 외부 모델 → 내 도메인 모델 변환
    }
}
```
- 인터페이스는 도메인에, 구현은 infra에 DIP
- RecSystemClient가 두 가지를 책임진다: ① 외부 시스템 연동(REST 호출) ② 모델 변환(RecommendationItem → Product).
- 변환 로직이 복잡해지면 별도 Translator 클래스로 분리 (단일 책임 분리).

**통합 방식 2 -- 간접 통합(메시지 큐)**
REST 직접 호출 대신 메시지 큐로 비동기 연동 
```java
// 카탈로그 기준 → 카탈로그 모델 그대로 전송
messageClient.send(new ViewLog(memberId, productId, time));

// 추천 기준 → 카탈로그가 추천 모델로 맞춰서 전송
messageClient.send(new ActivityLog(productId, memberId, ActivityType.VIEW, time));
```
동일하게 변환 비용은 존재한다. 다만 어느 쪽이 변환할지는 합의 필요
>BC 통합의 본질은 "남의 모델을 내 도메인에 들이지 않고, 경계에서 변환한다". 
>REST든 메시지 큐든 메커니즘만 다를 뿐, 변환 책임을 어디에 둘지를 정하는 게 설계의 핵심

### 바운디드 컨텍스트 간 관계

1. 한쪽이 API를 제공하고, 다른 쪽이 호출하는 관계
   - 하류는 상류에 의존한다. API가 바뀌면 하류 코드로 바뀐다.(영향 받음)
2. 공개 호스트 서비스
   - 상류 하나에 하류가 여러 개일때, 하류마다 따로 맞추면 문제 발생, 사율가 여러 하류 요구를 수용하는 단일 공개 API를 정의해야 함
   
3. 안티코럽션 계층
   - 상류 서비스는 상류 도메인 모델을 따른다. 그 모델이 하류 도메인을 오염시키지 않게 막는다.
   - 외부가 바뀌어도 내 도메인은 안 깨진다.

4. 공유 커널
   - 한 팀이 임의 변경 시 다른 팀에 영향을 준다. (단일 책임을 안지키는 거라 당연한듯?)
   - 두 팀이 밀접한 관계 필수
5. 독립 방식
   - 통합하지 않고 서로 독립적으로 사용, 책임 분리되지만 규모가 커지면 유지보수가 어렵다.

### 컨텍스트 맵
개별 바운디드 컨텍스트에 매몰되면 전체를 보지 못할 때가 있다.
중간 중간 바운디드 컨텍스트 간의 관게를 매핑하자 (공개 호스트 서비스, 안티코럽션 계층 표기해서)
> 이를 쉽게 만들고 관리하는 방법이 있을까?
