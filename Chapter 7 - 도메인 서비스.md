도메인 서비스는 도메인 영역에 위치한 도메인 로직을 표현할 때 사용한다.

- 계산 로직 : 여러 애그리거트가 필요한 계산 로직이나, 한 애그리거트에 넣기에는 다소 복잡한 계산 로직
- 외부 시스템 연동이 필요한 도메인 로직 : 구현하기 위해 타 시스템을 사용하는 도메인 로직 

도메인의 의미가 드러나는 용어를 타입과 메서드 이름으로 갖는다.

**애그리거트 객체에 도메인 서비스를 전달하는 것은 응용 서비스 책임이다**

### 도메인 서비스 객체를 애그리거트에 주입하지 말자
프레임워크가 제공하는 의존 주입 기능을 사용해서 도메인 서비스를 애그리거트에
주입해야 기술적으로 나은 것 같은 착각을 하게된다.

하지만 애그리거트가 제공하는 모든 기능에서 해당 도메인 서비스를 모두 사용하지 않는 경우가 대부분이고
필요로 하지 않는다. (특정 필드,메서드만 도메인 서비스를 필요로 함) 일부 기능을 위해 굳이 도메인 서비스 
객체를 애그리거트에 의존 주입할 필요가 있을까?

### 도메인 서비스의 기능을 실행할 때 애그리거트를 전달하는 경우 
예를 들어 계좌 이체 기능의 경우 
계좌 이체에는 두 계좌 애그리거트가 관여하는데 한 애그리거트는 금액을 출금하고
나머지 하나는 입금한다.

이때 응용 서비스는 두 `Account` 애그리거트를 구한 뒤에 해당 도메인 영역의 TransferService를 이용해서 계좌 이체 도메인 기능을 실행한다.

```java
public class TransferService {
	
	public void transfer(Account fromAcc, Account toAcc, Money amounts) {
		fromAcc.withdraw(amounts);
		toAcc.credit(amounts);
    }
}
```

특정 기능이 응용 서비스인지 도메인 서비스인지 감을 잡기 어려울 때는 해당 로직이 애그리거트의 상태를 변경하거나 애그리거트의
상태 값을 계산하는지 검사해 보면 된다.

### 도메인 서비스의 패키지 위치
도메인 서비스는 다른 도메인 구성요소와 동일한 패키지에 취치한다.

만약 도메인 서비스의 개수가 많거나 엔티티나 밸류와 같은 다른 구성요소와 명시적으로 구분하고 싶다면
`domain` 패키지 하위에 `model`, `service`, repository 와 같이 하위 패키지를 구분해도 된다.

- 도메인 서비스의 로직이 고정되어 있지 않은 경우 서비스 자체를 인터페이스로 구현해도 된다.
